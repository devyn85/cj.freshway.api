spring:
  datasource-oracle:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@//106.248.245.10:31521/ORCL
    username: LOGISONESVC
    password: wemeet5go
    hikari:
      connection-test-query: SELECT 1 FROM DUAL
      validation-timeout: 3000
      connection-timeout: 30000

  # 테스트 시 불필요한 설정 비활성화
  jmx:
    enabled: false

cf:
  excel:
    uploadPath: /tmp/excel-upload-test

logging:
  level:
    root: INFO
    cjfw: DEBUG
    cjfw.core: DEBUG
    cjfw.core.excel: DEBUG
